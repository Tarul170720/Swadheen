<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.swadheen">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/facebook_cover_photo_1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Swadheen">
        <activity android:name=".addImages"></activity>
        <activity android:name=".CroppedImageUserActivity" />
        <activity android:name=".CroppedImageUploadActivity" />
        <activity android:name=".ViewAllActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".Payment" />
        <activity android:name=".SubCategoryReguest" />
        <activity android:name=".WorkerSideSubCategory" />
        <activity android:name=".WorkStatus" />
        <activity android:name=".Face_detection_and_recogition" />
        <activity android:name=".ClassifierActivity" />
        <activity android:name=".CameraConnectionFragment" />
        <activity android:name=".results" />
        <activity android:name=".IntermediateQuestions" />
        <activity android:name=".BasicQuestions" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".QuestionAnswer" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".CostumerMapsActivity"
            android:label="@string/title_activity_costumer_maps" />
        <activity android:name=".SubCategoryActivity" />
        <activity android:name=".UserMainActivity" />
        <activity android:name=".NearbyWorkerProfile" />
        <activity android:name=".CostumerCompleteRegistration" />
        <activity android:name=".WorkerCompleteRegistration" />
        <activity android:name=".WorkerAtNearestLocation" />
        <activity android:name=".CostumerFristPage" />
        <activity android:name=".Costumer" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TaskCompleted" />
        <activity android:name=".TaskPending" />
        <activity android:name=".DriverProfile" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>